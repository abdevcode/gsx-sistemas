Per crear el script restaura_inici.sh a les vostres maquines, amb la diferencia que al Bastio, crec, la linia del sysctl no s'ha de posar:
-primer de tot configureu els cables correctament, un cop fet aixo, feu
cp -p /etc/network/interfaces router/

- Ara anem a /milax/router i creem el fitxer per al script "touch restaura_inici.sh"
- Dins hi poseu el seguent codi:
#! /bin/bash

echo "Iniciant configuracio"
cp interfaces /etc/network/interfaces

ifup enp0s8
sysctl net.ipv4.ip_forward=1

- Feu un ping a veure si us va.